#object <- new("VDJ",
#              celltype='abTCR',
#              species='Homo_sapiens',
#              clone_data=list())

#object <- readVDJ_10X(object,
#                      path = 'path/to/sample/vdj_t/')

#object = combineChains(object = object, handle_multiple = 'keep_all')

#VDJ(path = 'path/to/sample/vdj_t/',
#    celltype = 'abTCR', species = 'Homo_sapiens', input_origin = '10x')
